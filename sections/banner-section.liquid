{% comment %}
🎁 FIXED VERSION

* Top bar is slim.
* Banner image stays same height (50vh).
* Text ("The Gift Guide") is placed **on top of image**, positioned left and vertically centered into the empty white area.
  {% endcomment %}

<section class="banner-container">
  <!-- 1. Top Bar Area -->
  <div class="top-bar">
    <div class="logo-text">{{ section.settings.logo_text }}</div>
    <div class="header-tagline">{{ section.settings.header_tagline }}</div>
    <a href="{{ section.settings.button_url }}" class="shop-button" role="button">
      {{ section.settings.button_text }}
      <span class="arrow">→</span>
    </a>
  </div>

  <!-- 2. Image + Text Area Combined -->

  <div class="banner-image-area">
    <img src="{{ 'Frame.svg' | asset_url }}" alt="Frame Background" class="banner-bg-img" />
    <div class="banner-text-overlay">
      <h1 class="main-title">{{ section.settings.main_title }}</h1>
      <p class="description">{{ section.settings.description }}</p>
      <a href="{{ section.settings.main_button_url }}" class="shop-now-btn" role="button">
        {{ section.settings.main_button_text }} <span class="arrow">→</span>
      </a>
    </div>
  </div>
</section>

{% schema %}
{
"name": "Banner Section",
"settings": [
{
"type": "header",
"content": "Top Bar"
},
{ "type": "text", "id": "logo_text", "label": "Logo Text", "default": "TISSO VISON" },
{ "type": "text", "id": "header_tagline", "label": "Header Tagline", "default": "Find the ideal gift for your loved ones." },
{ "type": "text", "id": "button_text", "label": "Top Button Text", "default": "CHOOSE GIFT" },
{ "type": "url", "id": "button_url", "label": "Top Button Link" },
{
"type": "header",
"content": "Main Content"
},
{ "type": "text", "id": "main_title", "label": "Main Title", "default": "The Gift Guide" },
{ "type": "textarea", "id": "description", "label": "Description", "default": "Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season." },
{ "type": "text", "id": "main_button_text", "label": "Main Button Text", "default": "SHOP NOW" },
{ "type": "url", "id": "main_button_url", "label": "Main Button Link" }
],
"presets": [{ "name": "Banner Section" }]
}
{% endschema %}

{% stylesheet %}
.banner-container {
display: flex;
flex-direction: column;
background-color: #fff;
font-family: 'Inter', sans-serif;
color: #000;
}

/* 1. Top Bar Area */
.top-bar {
display: flex;
justify-content: space-between;
align-items: center;
width: 100%;
height: 50px; /* Slim */
padding: 6px 40px;
background-color: #F8F8F8;
border-bottom: 1px solid #EAEAEA;
position: relative;
z-index: 10;
}
.logo-text {
font-size: 20px;
font-weight: 700;
letter-spacing: 0.1em;
}
.header-tagline {
position: absolute;
left: 50%;
transform: translateX(-50%);
font-size: 15px;
white-space: nowrap;
}
.shop-button {
background-color: #ffe500;
color: #000;
font-weight: 700;
padding: 10px 24px;
border-radius: 4px;
text-decoration: none;
font-size: 15px;
display: inline-flex;
align-items: center;
gap: 8px;
transition: transform 0.3s ease;
white-space: nowrap;
}

/* 2. Image + Text Overlay */
.banner-image-area {
position: relative;
width: 100%;
height: 50vh; /* keep original */
min-height: 400px;
overflow: hidden;
}
.banner-bg-img {
width: 100%;
height: 100%;
object-fit: cover;
}
.banner-text-overlay {
position: absolute;
top: 50%;
left: 10%; /* adjust to move text into the empty space */
transform: translateY(-50%);
max-width: 500px;
display: flex;
flex-direction: column;
gap: 20px;
}
.main-title {
font-weight: 700;
font-size: 56px;
line-height: 1.1;
margin: 0;
}
.description {
font-size: 18px;
line-height: 1.6;
margin: 0;
}
.shop-now-btn {
background-color: #000;
color: #fff;
padding: 16px 40px;
border-radius: 4px;
font-weight: 700;
font-size: 16px;
text-decoration: none;
display: inline-flex;
align-items: center;
gap: 8px;
transition: transform 0.3s ease, background-color 0.3s ease;
}

/* Hover Effects */
.arrow { transition: transform 0.3s ease; }
.shop-button:hover, .shop-now-btn:hover { transform: translateY(-4px); }
.shop-button:hover .arrow, .shop-now-btn:hover .arrow { transform: translateX(6px); }
.shop-now-btn:hover { background-color: #333; }

/* Responsive */
@media (max-width: 990px) {
.top-bar { padding: 6px 20px; height: 45px; }
.main-title { font-size: 44px; }
}
@media (max-width: 768px) {
.header-tagline { display: none; }
.top-bar { padding: 6px 15px; height: 40px; }
.logo-text { font-size: 16px; }
.shop-button { font-size: 13px; padding: 8px 16px; }
.banner-text-overlay { left: 5%; max-width: 90%; text-align: center; align-items: center; }
.main-title { font-size: 38px; }
}
{% endstylesheet %}

{% javascript %}
document.addEventListener('DOMContentLoaded', () => {
console.log('Banner with overlay text loaded.');
});
{% endjavascript %}
