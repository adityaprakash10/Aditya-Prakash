{% comment %}
🎁 PIXEL-PERFECT BANNER - FINAL CORRECTED VERSION
- Structure is now three separate areas: Top Bar, Image Area, and Text Area below.
- "The Gift Guide" is now in its own white space and is a single line.
- Top Bar is now the correct narrow height.
- REMINDER: Manually change "Top Button Text" to "CHOOSE GIFT" in the theme customizer.
{% endcomment %}

<section class="banner-container">
  <!-- 1. Top Bar Area -->
  <div class="top-bar">
    <div class="logo-text">{{ section.settings.logo_text }}</div>
    <div class="header-tagline">{{ section.settings.header_tagline }}</div>
    <a href="{{ section.settings.button_url }}" class="shop-button" role="button">
      {{ section.settings.button_text }}
      <span class="arrow">→</span>
    </a>
  </div>

  <!-- 2. Image Area -->
  <div class="banner-image-area">
    <img src="{{ 'Frame.svg' | asset_url }}" alt="Frame Background" class="banner-bg-img" />
  </div>

  <!-- 3. Text Content Area -->
  <div class="banner-text-area">
    <div class="banner-content">
      <h1 class="main-title">{{ section.settings.main_title }}</h1>
      <p class="description">{{ section.settings.description }}</p>
      <a href="{{ section.settings.main_button_url }}" class="shop-now-btn" role="button">
        {{ section.settings.main_button_text }} <span class="arrow">→</span>
      </a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Banner Section",
  "settings": [
    {
      "type": "header",
      "content": "Top Bar"
    },
    { "type": "text", "id": "logo_text", "label": "Logo Text", "default": "TISSO VISON" },
    { "type": "text", "id": "header_tagline", "label": "Header Tagline", "default": "Find the ideal gift for your loved ones." },
    { "type": "text", "id": "button_text", "label": "Top Button Text", "default": "CHOOSE GIFT" },
    { "type": "url", "id": "button_url", "label": "Top Button Link" },
    {
      "type": "header",
      "content": "Main Content"
    },
    { "type": "text", "id": "main_title", "label": "Main Title", "default": "The Gift Guide" },
    { "type": "textarea", "id": "description", "label": "Description", "default": "Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season." },
    { "type": "text", "id": "main_button_text", "label": "Main Button Text", "default": "SHOP NOW" },
    { "type": "url", "id": "main_button_url", "label": "Main Button Link" }
  ],
  "presets": [{ "name": "Banner Section" }]
}
{% endschema %}

{% stylesheet %}
.banner-container {
  display: flex;
  flex-direction: column;
  background-color: #fff;
  font-family: 'Inter', sans-serif;
  color: #000;
  /* The total height will be determined by its content */
}

/* 1. Top Bar Area */
.top-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: relative;
  width: 100%;
  padding: 10px 60px; /* CORRECTED: Very narrow padding */
  flex-shrink: 0;
  background-color: #F8F8F8;
  border-bottom: 1px solid #EAEAEA;
  z-index: 10;
}
.logo-text {
  font-size: 24px;
  font-weight: 700;
  letter-spacing: 0.1em;
}
.header-tagline {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  font-size: 16px;
  white-space: nowrap;
}
.shop-button {
  background-color: #ffe500;
  color: #000;
  font-weight: 700;
  padding: 14px 32px;
  border-radius: 4px;
  text-decoration: none;
  font-size: 16px;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: transform 0.3s ease;
  white-space: nowrap;
}

/* 2. Image Area */
.banner-image-area {
  width: 100%;
  height: 50vh; /* Adjust this value to show more/less of the image */
  min-height: 400px;
  overflow: hidden;
}
.banner-bg-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* 3. Text Content Area */
.banner-text-area {
  width: 100%;
  padding: 5% 12%; /* Use percentages for responsive padding */
  background-color: #fff;
}
.banner-content {
  max-width: 550px;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  text-align: left;
  gap: 25px;
}
.main-title {
  font-weight: 700;
  font-size: 56px; /* CORRECTED: Smaller size to ensure one line */
  line-height: 1.1;
  margin: 0;
  white-space: nowrap; /* Ensures it stays on one line */
}
.description {
  font-size: 18px;
  line-height: 1.6;
  max-width: 500px;
  margin: 0;
}
.shop-now-btn {
  background-color: #000;
  color: #fff;
  padding: 16px 40px;
  border-radius: 4px;
  font-weight: 700;
  font-size: 16px;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  transition: transform 0.3s ease, background-color 0.3s ease;
}

/* Animations */
.arrow { transition: transform 0.3s ease; }
.shop-button:hover, .shop-now-btn:hover { transform: translateY(-4px); }
.shop-button:hover .arrow, .shop-now-btn:hover .arrow { transform: translateX(6px); }
.shop-now-btn:hover { background-color: #333; }

/* Responsive Adjustments */
@media (max-width: 990px) {
  .top-bar { padding: 10px 30px; }
  .main-title { font-size: 48px; }
  .banner-text-area { padding: 5% 8%; }
}
@media (max-width: 768px) {
  .header-tagline { display: none; }
  .top-bar { padding: 10px 20px; }
  .logo-text { font-size: 18px; }
  .shop-button { font-size: 14px; padding: 10px 18px; }
  .banner-text-area { padding: 8% 5%; }
  .banner-content { align-items: center; text-align: center; }
  .main-title { font-size: 42px; }
}
{% endstylesheet %}

{% javascript %}
// Vanilla JS - no jQuery needed.
document.addEventListener('DOMContentLoaded', () => {
  console.log('Final banner with correct structure loaded.');
});
{% endjavascript %}